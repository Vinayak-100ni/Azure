🔹 Intra-Region VNet Peering (Same Region)
Steps (Azure Portal)

Azure Portal → Virtual Networks

Select VNet-A

Go to Settings → Peerings

Click + Add

Enter:

Peering name (VNetA-to-VNetB)

Remote VNet: VNet-B

Configure options:

✔ Allow virtual network access

(Optional) Allow forwarded traffic

(Optional) Allow gateway transit

Click Add

Repeat the same steps from VNet-B → VNet-A

✅ Peering status should be Connected

🔹 Inter-Region (Global) VNet Peering
Steps (Same as Intra-Region)

Azure Portal → Virtual Networks

Select VNet-A (Region 1)

Go to Settings → Peerings

Click + Add

Select Remote VNet (Region 2)

Enable:

✔ Allow virtual network access

Click Add

Repeat from VNet-B → VNet-A

✅ Global peering is created automatically
