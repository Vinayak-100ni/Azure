🟢 STEP 1: Create Private DNS Zone

Azure Portal → Private DNS zones

Click + Create

Configure:

Name:

privatelink.database.windows.net


(example — depends on service)

Resource group

Create

🟢 STEP 2: Link Private DNS Zone to VNet

Open Private DNS Zone

Go to Virtual network links

Click + Add

Configure:

Link name: vnet-link

Virtual network: Select VNet

✔ Enable auto-registration (for VMs)

Save

🟢 STEP 3: Create Private Endpoint (If not already created)

Azure Portal → Private Endpoint

Click + Create

Select:

Target resource (DB / Storage / App)

Sub-resource (SQL / Blob / Web)

Networking:

Select VNet + Subnet

DNS:

✔ Integrate with Private DNS zone

Create

🟢 STEP 4: Verify DNS Records

Open Private DNS Zone

Go to Record sets

Verify:

A record created automatically

Maps service name → private IP

Example:

mydb.privatelink.database.windows.net → 10.0.2.4

🟢 STEP 5: Test Name Resolution

From VM inside VNet:

nslookup mydb.database.windows.net


Expected:

Resolves to private IP

Not public IP

🔁 Optional: Manual Record Creation

Private DNS Zone → Record sets

Click + Add

Type:

A record

Name: app

IP: 10.0.3.5
